apiVersion: v1
kind: Service
metadata:
  name: {{ include "familyshare-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "familyshare-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "familyshare-service.selectorLabels" . | nindent 4 }}
  ports:
    # Service port == container port so in-cluster DNS is http://fs-<svc>:<port>.
    - name: http
      port: {{ .Values.port }}
      targetPort: http
      protocol: TCP
