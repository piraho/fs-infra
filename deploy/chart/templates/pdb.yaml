{{- if .Values.pdb.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "familyshare-service.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "familyshare-service.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.pdb.minAvailable }}
  selector:
    matchLabels:
      {{- include "familyshare-service.selectorLabels" . | nindent 6 }}
{{- end }}
